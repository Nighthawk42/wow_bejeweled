# Batch 10: lines 4501–5000

Source: `Legacy/Bejeweled_Mainline.lua` at commit `6faec1c`  
Coverage: lines 4501–5000 inclusive  
Blank: 4921, 4970  
Status: complete; shadowing `Se` closes at 4920, animator factory `A` closes at 4969, and network factory `V` continues into batch 11

## Line-indexed coverage audit

| Lines | Evidence represented |
| ---: | --- |
| 4501–4508 | Finishes big-star detonation's 3×3/upward propagation branch. If no prior delay exists it sets `explodeDelay = 1`; either way the effect prevents animation-state advancement by clearing completion flag `w`. |
| 4509–4522 | Non-exploding big-star branch advances shrink frame `i`: completion clears effect/contents and re-renders; otherwise it suppresses state advancement and scales texture width/height by remaining frames over `se`. |
| 4523–4586 | In drop state `pe`, integrates downward velocity using `P`, clamps at `maxYOffset`, commits final Y position/effect state, plays GemClick, and reanchors. Spawn effect `W` clips the gem texture as it crosses the board top, then restores normal/hyper/explode presentation, pending `spawnHyper`, big star, and lightwave visibility. Atlas cropping derives height and UV top from visible pixels over `p`/255. |
| 4587–4617 | Applies one generic falling-motion step to objects not already moved this tick, including acceleration proportional to existing speed plus `P`; clamps/commits finite targets or keeps open-ended falling, resets landing state, plays GemClick, and reanchors. |
| 4618–4647 | Hyper effect `g` advances atlas `O`, then adds glow intensity to valid cells in a 5×5 neighborhood: outer ring +1, inner region +2, capped at `z`; selected/off-board cells hide glow, others use oscillating scale `H`. |
| 4648–4697 | Big-star effect `ye` advances two opposing rotation frames, returns ended stars to `bigStarQueue`, rotates texture/highlight, oscillates blend alpha between 0 and 100, and applies the same 5×5 glow field around its parent. Secondary frame `frame2` is an undeclared implicit global in this source. |
| 4698–4729 | Shard effect `He` advances atlas `ie`, integrates X/Y velocity using gravity `k`, accelerates horizontal drift up to magnitude 8, fades after vertical threshold `oe`, and returns finished objects to `shardQueue`. |
| 4730–4745 | Explosion effect `Oe` advances atlas `J` until `bt` or `fxEnd`, then hides and returns the object to `explosionQueue`. |
| 4746–4766 | Lightning effect `We` advances to `Rt`, alternates highlight alpha by frame parity, honors `fxEnd`, then hides texture/highlight and returns the object to `lightningQueue`. |
| 4767–4786 | Floating-text effect `te` advances/fades over `be`, moves non-score text down and score text up using step `I`, anchors against game board `K`, and returns finished text to `floatingTextQueue`. |
| 4787–4806 | Effect `Ve` waits until `yt`, then shows and oscillates vertical `bounceY` between 0 and 10 before reanchoring. No terminal transition appears in this branch. |
| 4807–4882 | Swap movement `_e` advances X/Y offsets using step `_`, clamps at cell size `ue`, and uses local completion flag `e`. Invalid swaps reverse once (`1`→`2`) then finalize on return; valid completion clears movement vectors/selector, reanchors big stars, triggers hyper/hyper scoring or a pending hyper clear, decrements `movingJewels`, re-renders, and moves animator state to `Pe` after both gems finish. |
| 4883–4904 | Movement `at` integrates free-flight X/Y and gravity `k`, fades near terminal velocity, then clears/hides and resets positions when finished; active objects suppress state advancement. |
| 4905–4920 | Removes finished non-lightwave objects from mutable stack or advances index `f`, invokes new-game countdown when active, calls animator status change only when completion flag `w` remains true, resets elapsed time, and closes `Se`. This resolves the full animator update across lines 4235–4920. |
| 4921 | Blank. |
| 4922–4937 | New `A` shadows effect constant `A` and constructs the invisible animator frame: initializes stack/timing/glow state, attaches `Se` as `OnUpdate`, `ze` as `OnEvent`, registers `PLAYER_ENTERING_WORLD`, and sets initial animation state `V`. Field `movingGems` is initialized, while swap code reads/writes distinct `movingJewels`. |
| 4938–4969 | Precomputes sine/cosine rotation tables for degrees 0–360 using shared chunk scratch `i`, initializes all effect queues and spawn arrays, and attaches factories/helpers. These assignments definitively name `de`/`Le`/`X`/`ne` as `HandleAnimatorStatusChange`, `HandleJewelDropping`, `HandleJewelFalling`, and `HandleNewGameCountdown`; returns the animator and closes `A`. |
| 4970 | Blank. |
| 4971–4987 | New `V` shadows idle animation-state constant and begins network-frame construction. It registers dynamic prefix `xe` plus `BEJ2a`, creates `BejeweledNetwork`, initializes queue/throttle state, and installs `Send`, which queues a `+`/`~`-delimited four-field payload only when `scoresUpdated` is true. |
| 4988–5000 | Begins network `OnUpdate`: handles deferred level-bar resize, accumulates elapsed delta, and returns until one second has passed. The callback and factory `V` remain open into batch 11. |

## Findings

- `Se` resolves as the animator `OnUpdate`. Its per-tick `w` is the phase-complete flag: non-idle phases begin eligible to advance, and any still-active effect clears it; only a surviving true value calls `HandleAnimatorStatusChange`.
- The animator factory provides explicit helper ownership and names. Its `movingGems` initialization does not match the `movingJewels` field used by swap scheduling/completion, a preserved defect candidate.
- Physics roles are now closed: `k` is gravity for shards/free flight, `_` is swap offset step, `P` is board-drop acceleration, and `I` is floating-text frame/position step.
- Hyper and big-star glow fields use a capped weighted 5×5 neighborhood with oscillating alpha. Big-star rotation additionally leaks `frame2` as an implicit global.
- Network construction registers both the mutable legacy/migrated prefix `xe` and literal `BEJ2a`; after migration those values can be identical. The send wire format is four fields separated as `type+field~field~field`.

## API and architecture mapping

- Frame templates/scripts/events, `UIFrameFade`, atlas manipulation, and `C_ChatInfo.RegisterAddonMessagePrefix` are legacy contracts requiring authoritative Retail verification before implementation.
- `UI/Animator.lua`: complete `Se`, animator factory `A`, effect queues, motion/UV/glow derivations, and helper attachment.
- `Engine/Swaps.lua`: invalid reversal, hyper completion, and `movingJewels` coordination.
- `Network/Transport.lua`: open factory `V`, prefix registration, queue format, and throttled update.

## Forward dependencies

Batch 11 must continue at line 5001 inside the network `OnUpdate`, resolve its throttling/send/event behavior and all open `V` locals only from the completed factory, and preserve both prefix registration and exact delimiter/wire behavior until all consumers are analyzed.
