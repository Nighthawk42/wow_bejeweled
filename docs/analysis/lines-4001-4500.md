# Batch 09: lines 4001–4500

Source: `Legacy/Bejeweled_Mainline.lua` at commit `6faec1c`  
Coverage: lines 4001–4500 inclusive  
Blank: 4040, 4092, 4138, 4234  
Status: complete; shadowing `Le` closes at 4039 and shadowing `Se` continues into batch 10

## Line-indexed coverage audit

| Lines | Evidence represented |
| ---: | --- |
| 4001–4027 | Finishes `Le` hyper restoration. Each pending hyper tries six random cells that contain neither a big star nor another pending hyper; the fallback scans the board cyclically. Successful cells receive `spawnHyper`, and `n.hyperSpawn` is reset. |
| 4028–4039 | Clears level-spawning state, advances the level through `Ue` unless this is initial-game startup, checks the 1,000-gems achievement, and closes `Le`. The final `s` binding and post-fill local `d` are never read; `Le` resolves as `spawnAndRefillBoard` across lines 3874–4039. |
| 4040 | Blank. |
| 4041–4091 | New `X` shadows the base-70 decoder and collapses gems into empty cells. It scans bottom-up, finds the nearest nonempty cell above, transfers contents and optional `bigStar`, clears the source, derives fall distance/velocity/effect state, transfers deferred selection, re-renders both cells, adds the target to animator `r`, and reanchors it. |
| 4092 | Blank. |
| 4093–4119 | New `ne` shadows half-gem-height constant and drives new-game countdown state. Each elapsed second resets the hint frame, decrements 3/2/1, plays GetReady/Go, shows status text, creates a legal-move hint, and starts the non-classic level timer when countdown completes. |
| 4120–4137 | Builds a three-second fade-out descriptor with completion callback that restores alpha and hides status/background, applies it with `UIFrameFade`, enables moves, and closes countdown handler `ne`. |
| 4138 | Blank. |
| 4139–4147 | New `de` shadows random-gem assignment and advances animator state from parameter `o`. It clears the spawn guard at state `pe`, normalizes new-game state `ot` to idle `V`, and enters match/transition processing at `ot` or states above `Fe`. |
| 4148–4165 | Calls `he`; a match or forced explosion moves to clear state `Pe`. Otherwise it awards clear-10/15/20/25 skills from `gemsCleared` and resets the counter. Local `o` declared with match result `i` shadows the already-consumed parameter and later carries a legal-move candidate. |
| 4166–4195 | Handles level-up transition by setting level-spawn state and calling `Ke`; otherwise it shows game-over summary or selects idle/drop state, clears spawn flags, hides/fades level-up text, and restores move permission when appropriate. |
| 4196–4233 | Updates current/profile largest combo, applies deferred selection, resets combo/explosion counts, asks `Q` for another legal move, ends an unsolvable game or creates the next hint, updates classic-mode availability via `St`, applies deferred mouse-over, and closes animation-state handler `de`. |
| 4234 | Blank. |
| 4235–4243 | New `Se` shadows selection-clear helper and begins the main animator update for animator `l` and elapsed delta `w`. It throttles by `l.delay` and stops animation work while the addon is hidden. This function remains open at line 4500. |
| 4244–4272 | Binds animator/board references and elapsed-scaled motion constants, initializes the later animation-object binding `t` to 20 (overwritten before read), captures the animation stack/state and per-tick trigger guards, oscillates `glowFrame` between 1 and `re`, and derives normalized glow offset `H`. The first `t` at 4250 is shadowed before use. |
| 4273–4294 | Every 25 elapsed units emits lightwaves across the first column, clears every board gem's glow, initializes stack index `f`, and drains `newJewel` queue entries with `B`. |
| 4295–4305 | Derives whether animation state is non-idle, invokes jewel dropping/falling phase handlers, and begins iterating `animationStack` through local index `f`; loop variable `Y` counts the original stack length while removals adjust `f`. |
| 4306–4325 | Advances delayed lightwave effect `De`: at frame 1 it spawns the wave to the right and selects atlas cell `N[2]`; at frame 10 it returns the object to `lightwaveQueue`, hides/detaches it, and otherwise fades alpha according to parent visibility. |
| 4326–4402 | In idle state `V`, advances selection effect `y`, shine-chain effect `je`, and adjacent-highlight effect `ke`. Shine chains compute adjacent coordinates from `FX_SHINE_GRIDX/Y`, alternate paired keyframes, schedule eligible neighbor gems, maintain `adj`, and remove ended effects from the stack while clearing highlight UV/alpha. |
| 4403–4456 | In clear state `Pe`, converts score objects to floating text and applies score/combo sound; finalizes pending power or hyper gem creation with first-use tips; and serializes hyper triggers through per-tick guard `G`, one-tick delay, `Ce`, clear/render fallback, and flag `w`. |
| 4457–4500 | Begins big-star explosion handling, serialized by guard `v`. When `explodeDelay` is set it hides/releases the star, captures `forcedExplode`, queues an explosion, and walks a 3×3 neighborhood to create shards, set vertical velocities, enqueue falling gems, and compensate `gemsCleared` for forced explosions. For the upper neighbor row it also propagates motion through every cell above. The nested explosion branch and `Se` continue into batch 10. |

## Findings

- `Le` resolves as the board spawn/refill controller. It guarantees a legal starting/refilled board by bounded rerolls, restores counted special gems without overlapping them, advances levels, and gates the 1,000-gem achievement.
- Lua lexical capture is behavior-critical: `Le` retains the earlier random-gem `de` and half-height `ne`; gravity `X` retains the earlier selection-clear `Se` after those spellings are shadowed later in this batch.
- Shadowing `X` is gravity compaction, distinct from the earlier decoder. It moves logical contents and big-star ownership into fixed cell frames rather than moving frame identity through the grid.
- Shadowing `de` is the state-transition coordinator between matching, clearing, level spawning, game-over, idle play, hints, and deferred selection/mouse-over.
- Shadowing `Se` is the central animator update loop. Its animation stack uses mutable index `f` while loop counter `Y` preserves the original traversal count, allowing in-place removal through `B`.
- Shine propagation uses paired odd/even `nextGlow` values and coordinate/keyframe lookup tables; big-star detonation combines a 3×3 shard clear with upward-column motion propagation.

## API and architecture mapping

- `UIFrameFade`, `UIFrameFadeOut`, frame/texture anchoring, alpha, and visibility calls are legacy UI contracts requiring authoritative Retail verification before implementation.
- `Engine/BoardSpawn.lua`: completed `Le`; `Engine/Gravity.lua`: shadowing `X`.
- `Engine/StateMachine.lua`: shadowing `de`; `Engine/Countdown.lua`: shadowing `ne`.
- `UI/Animator.lua`: open `Se`, animation-stack mutation, idle shine/lightwave effects, and clear-phase special-gem dispatch.

## Forward dependencies

Batch 10 must continue at line 4501 inside `Se`'s big-star explosion branch, finish every remaining animation type and physics/state transition before resolving its boundary-dependent locals, and preserve stack-removal order and per-tick guards exactly.
