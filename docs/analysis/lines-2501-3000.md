# Batch 06: lines 2501–3000

Source: `Legacy/Bejeweled_Mainline.lua` at commit `6faec1c`  
Coverage: lines 2501–3000 inclusive  
Blank: 2507, 2518, 2544, 2565, 2584, 2603, 2622, 2644, 2699, 2730, 2748, 2756, 2769, 2799, 2841  
Status: complete; `tt` closes at 2506 and match scanner `he` continues into batch 07

## Line-indexed coverage audit

| Lines | Evidence represented |
| ---: | --- |
| 2501–2506 | Finishes `tt`: resets glow/effect/frame and returns completed gem frame. |
| 2507 | Blank. |
| 2508–2517 | `It` configures a gem as contents-9 hyper gem using atlas `O`, white shine, effect `g`. Unused `t` parameter. |
| 2518 | Blank. |
| 2519–2543 | `Ht` obtains/creates pooled big-star image, sizes layers, anchors to parent gem, initializes effect/alpha fields, returns it. |
| 2544 | Blank. |
| 2545–2564 | `Ot` obtains/creates explosion image, positions from source gem, resets atlas/effect, plays Explosion, returns it. |
| 2565 | Blank. |
| 2566–2583 | `Et` lazily creates/reset hint arrow, optionally positions above gem, initializes bounce/effect, returns it. |
| 2584 | Blank. |
| 2585–2602 | `vt` obtains/creates lightwave child, copies parent highlight texture, resets atlas/alpha/frame delay, stores parent reference (legacy key typo `lightWaveObv`). |
| 2603 | Blank. |
| 2604–2621 | New `lt` function shadows dimension constant and obtains/creates lightning textures, colors by gem type, sets effect. `DrawRouteLine` is commented as removed, so actual route geometry/show is missing while highlight is shown. |
| 2622 | Blank. |
| 2623–2643 | New `qe` function shadows dimension constant and obtains/creates colored gem shard with random atlas frame and zero velocity. |
| 2644 | Blank. |
| 2645–2698 | New `Qe` function shadows dimension constant and clears/explodes a gem. Big stars queue explosion/scoring; normal/hyper contents clear/render; directional clears spawn ten randomized shards and append them directly to animation stack. |
| 2699 | Blank. |
| 2700–2729 | New `Je` function shadows dimension constant and obtains/creates floating text, applies score/non-score color, position/state, returns hidden object. |
| 2730 | Blank. |
| 2731–2747 | `CreateCaption` creates font string using bundled or standard font, color/position/text, returns it. |
| 2748 | Blank. |
| 2749–2755 | `Ye` mouse-over wrapper stores deferred target when moves disallowed, otherwise updates immediately. |
| 2756 | Blank. |
| 2757–2768 | `UpdateMouseOver` queues eligible gem highlight animation and resets glow/adjacency fields. |
| 2769 | Blank. |
| 2770–2798 | New `Ue` function shadows dimension constant and advances level: recomputes multiplier/threshold, updates HUD, highest level, level-10/15 skills, bar. |
| 2799 | Blank. |
| 2800–2840 | New `Ce` function shadows mode multiplier table and triggers hyper-gem chain: shards/score/clear source, scans grid for next same-color gem, queues it and lightning, clears trigger contents. Earlier closures retain multiplier table. |
| 2841 | Blank. |
| 2842–2884 | `he` begins full match scan. Traverses grid, skips empty/hyper, scans downward run and searches horizontal cross-arm while tracking bounds. Numerous duplicate `t` locals are minifier artifacts. |
| 2885–2937 | For vertical run ≥3, marks vertical and optional cross-arm clears, counts explosions, creates power gem for four/T shape or hyper gem for five+, reports match score through `M`, accumulates cleared count. |
| 2938–2972 | Resets run count and performs symmetric horizontal scan with vertical cross-arm detection. |
| 2973–3000 | For horizontal run ≥3, marks horizontal/cross clears, determines power/hyper creation and fallback random placement. Function continues in batch 07. |

## Findings

- `tt` resolves as gem-frame constructor. `It`, `Ht`, `Ot`, `Et`, `vt`, shadowing `lt`, shadowing `qe`, shadowing `Qe`, and shadowing `Je` are effect-object factories/clear helpers backed by explicit pools.
- Removed `DrawRouteLine` leaves the Ultra-gem lightning effect incomplete in this legacy snapshot; modernization needs supported geometry rather than restoring the obsolete global.
- `Ue` is level advancement. Shadowing `Ce` is hyper-chain processing, distinct from the earlier mode multiplier table captured by older closures.
- `he` is the match detector/marker. It handles straight runs and T/L intersections, creates power gems for four/intersections and hyper gems for five+, and delegates scoring to `M`.
- Effect objects share `fxType`, `fxFrame`, positions, alpha, velocities, and pool-specific fields; shard creation bypasses animator `Add` and inserts into the stack directly.

## API and architecture mapping

- Frame/texture/font APIs remain legacy stateful UI calls; `CreateCaption` uses Lua-5.1-compatible behavior.
- The removed `DrawRouteLine` call is explicit migration evidence. No `C_Timer`, metadata, animation-group, backdrop, or compartment API appears.
- `Engine/Matches.lua`: `he`, clear marking, power/hyper decisions.
- `Engine/Scoring.lua`: `Ue`, match reports.
- `UI/GemPool.lua`: `tt`, hyper rendering.
- `UI/Animations.lua`: all pooled effect factories, shard/lightning/hint behavior.

## Forward dependencies

Batch 07 must finish the horizontal branch of `he`, establish its return/state transitions, and trace effect dispatchers. Shadowed spellings must remain declaration-specific; no older closure binding may be rebound during modernization.
