# Batch 16: lines 7501–8000

Source: `Legacy/Bejeweled_Mainline.lua` at commit `6faec1c`  
Coverage: lines 7501–8000 inclusive  
Blank: 7632, 7797, 7815, 7832, 7873, 7994, 7997  
Status: complete; About factory `m`, `Initialize_OptionsScreen`, and event dispatcher `S` close, while function `k` continues into batch 17

## Line-indexed coverage audit

| Lines | Evidence represented |
| ---: | --- |
| 7501–7559 | Finishes About tab-1 tutorial content as three 376×118 bordered panels. Each combines `largeText` metadata/`artPieces` texture with centered instructions for swapping, four-gem Power Gems, and five-gem Hyper Cubes; Tutorial 2 uniquely supplies eight texture coordinates. |
| 7560–7598 | Builds About tab-2 PopCap story content: bordered panel, cropped PopCap art, left-justified origin narrative, maintained GitHub URL, and `Bejeweled.version`. |
| 7599–7631 | Builds About tab-3 credits content with title, original/maintainer credits, special thanks, beta testers, custom fonts/justification, then closes About factory `m`. |
| 7632 | Blank. |
| 7633–7664 | `Initialize_OptionsScreen` applies profile alpha to the main window and seeds fade-in/out endpoints. It creates/installs the board-parented Options screen/backdrop; show pauses and makes game/summary/hint transparent, while hide restores their alpha. |
| 7665–7715 | Adds Options title/content panel and keybind caption/button. Button toggles main-window keyboard capture, highlight, saved label, and modifier/new-key state; existing profile binding is applied with `SetOverrideBindingClick`. Game and mouse-off alpha sliders update fade descriptors, and game alpha immediately updates the window. |
| 7716–7734 | Reuses local `t` as vertical offset 100 and creates sound radio handler. It derives setting key from checkbox name, clears all three sound modes, sets selected mode to numeric `1`, and checks the clicked control. Creates Normal/Quiet/Off radio controls. |
| 7735–7783 | Adds Hide Minimap and Disable Hints callbacks plus Lock Window, chat skill gains/detail, guild rank publication, new game on flight, score publication/deduplication, and flight-tooltip settings. Minimap callback toggles frame visibility; hint callback creates a hint when enabled or sets hidden hint effect `S`, alpha/visibility/history when disabled. |
| 7784–7796 | Adds Auto-Open/Auto-Close headings and paired settings for flight start/end, death/ready check, and login/combat. |
| 7797 | Blank. |
| 7798–7814 | Installs main-window `OnKeyUp`. For modifier keys, it clears and rebuilds `keybindModifier` in ALT-CTRL-SHIFT order using `-` separators and current keyboard state; a local `t` shadows the consumed key parameter. |
| 7815 | Blank. |
| 7816–7831 | Begins main-window `OnKeyDown`. Escape stops capture, resets button/profile binding, and clears overrides. Screenshot bindings execute normally and return; `UNKNOWN` returns. |
| 7832 | Blank. |
| 7833–7872 | Modifier key-down normalizes away leading `L`/`R`, appends unique modifier tokens, and returns. Already-captured keys are ignored. Otherwise it rebuilds modifiers, composes final binding, disables capture, updates/unlocks button, applies override click to `BejeweledShowHideButton`, persists it, closes callback and Options initializer. |
| 7873 | Blank. |
| 7874–7889 | New `S` shadows inactive-hint effect constant and begins the central window event dispatcher. `UNIT_FLAGS` for player starts taxi flow and optional auto-open/unpause; friend/guild roster updates set network refresh flags. |
| 7890–7932 | `PLAYER_CONTROL_GAINED` ignores one zone-switch transition, then when a flight timer exists hides/reset timed flight UI, persists learned times, unregisters flight events, reconciles flight-learning elapsed time when difference exceeds one second, stops timer/countdown via `Me`, clears learning/timer state, and optionally starts game-over-style window fade/overlay close. Local flight-mode boolean `t` is computed but unused. |
| 7933–7972 | `PLAYER_MONEY` updates flight times; `VARIABLES_LOADED` invokes saved-variable initialization; leaving/entering world tracks zone/login state. First entry builds duplicated rank-name-prefixed classic/timed publish payload, refreshes guild roster, sends LogSync/HSPub to guild and online friends, shows legal notice or resets splash, removes `ShowLegal`, and honors open-on-login. Initial numeric scores and player checksum are overwritten/unused. |
| 7973–7993 | Death may open the window; ready check may close it unless popup is visible; combat increments pause stats for active unpaused games and may close the window. Dispatcher `S` closes. |
| 7994 | Blank. |
| 7995–7996 | New `k` shadows flight-learning game-mode constant and begins a function by forcing `Bejeweled.skillLimit = true`. |
| 7997 | Blank. |
| 7998–8000 | Comments state removed `GetMapContinents` previously could clear the skill limit; commented conditional remains open textually while function `k` continues into batch 17. |

## Findings

- About construction is complete with tutorial, origin/version, and credits tabs.
- Keybinding capture lives on the main window, not the Options frame. It preserves Screenshot, ignores unknown/repeated input, supports ordered modifiers, and maps the final binding to the hidden global show/hide button.
- Options show/hide owns only pause-on-show and alpha restoration; it does not unpause on hide, consistent with menu handoff ownership elsewhere.
- Sound mode is represented by mutually exclusive truthy numeric `1` fields. Several other checkboxes rely on the generic truthy-only settings write documented earlier.
- Flight completion synchronizes learning timers only when elapsed values differ by more than one second, then clears all learning state before optional fade-close.
- Login publication constructs `rankName*classic*rankName*timed`; the rank/name prefix is intentionally duplicated. Earlier numeric score conversions and player-name checksum local are dead before payload construction.
- Removed continent detection leaves `skillLimit` forced true at the Batch 16 boundary; the completed effect is deferred until `k` closes in Batch 17.

## API and architecture mapping

- Override bindings, binding execution, keyboard propagation, frame fade, friend/guild APIs, event names, taxi state, and removed map APIs require authoritative Retail verification before implementation.
- `UI/About.lua`: completed About factory and all three content tabs.
- `UI/Options.lua`: completed Options construction, settings controls, alpha/minimap/hint effects, and keyboard binding capture.
- `Core/Events.lua`: completed window event dispatcher for flight, persistence startup, networking/login, death, ready check, and combat.
- `Core/Compatibility.lua`: open function `k` and forced skill-limit fallback after map API removal.

## Forward dependencies

Batch 17 must resume at line 8001 inside function `k`, complete the final 401 source lines through line 8401, and only then assess the full-analysis phase gate and remaining unresolved behavior-critical identifiers.
