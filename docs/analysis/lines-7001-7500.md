# Batch 15: lines 7001–7500

Source: `Legacy/Bejeweled_Mainline.lua` at commit `6faec1c`  
Coverage: lines 7001–7500 inclusive  
Blank: 7412  
Status: complete; Feats of Skill factory `u` closes, while About-screen factory `m` continues into batch 16

## Line-indexed coverage audit

| Lines | Evidence represented |
| ---: | --- |
| 7001–7003 | Closes tab-1 `UpdateSkillScreen` and attaches it as tab-1 content `OnShow`. |
| 7004–7037 | Reassigns `i` to hidden tab-2 Personal Bests/Fun Statistics content. On show, it formats classic/timed bests, levels/moves/cascade/combo, played durations, combat pauses, gem/game/special totals, and favorite gem color. Favorite selection scans maximum counts but reports `NONE` only when color 1 remains maximum and ties color 2; other ties are not detected. Stores tab-2 content. |
| 7038–7087 | Builds Personal Bests title/panel, six labels, and right-anchored values for classic score, timed PPS, highest classic level, timed moves, largest cascade, and largest combo. Outer `n` is reused as vertical offset and a new local `r` as 16-pixel font size. |
| 7088–7154 | Builds Fun Statistics title/panel with nine labels and values: total/classic/timed play time, combat entries, favorite color, matched gems, account-wide games `K`, Hyper Cube total, and Power Gem total. Stores every value font string on tab-2 content for show-time refresh. |
| 7155–7193 | Reassigns `i` to hidden leaderboard tab-3 content. Its show callback selects friends/guild from frame ID and calls attached `nt` as `PopulateScores`. A local radio callback clears both checkboxes, checks the clicked one, copies its ID to parent, and hide/shows parent to refresh. Friends ID 3 and Guild ID 4 controls are styled/stored; Guild begins checked. |
| 7194–7251 | Creates classic and timed leaderboard headings/panels. Each ten-row loop builds ordinal, rank icon, placeholder name, and right-aligned placeholder score, storing rank/name/score objects under indexed keys consumed by `nt`. |
| 7252–7272 | Reassigns `i` to hidden tab-4 achievement content with ID 4. An initial copied show callback would call absent `PopulateScores("guild")`, but line 7410 later replaces this script before normal use. Adds multi-line achievement instructions/count caption and stores its format string. |
| 7273–7323 | Builds achievement list panel and another `ScrollFrame` whose `"UIPanelScrollFrameTemplate" and "BackdropTemplate"` expression again supplies only `BackdropTemplate`. Preallocates scaled plate rows for skill-data types 6–7 with title, description, plate texture, dimmed icon, and `hasData`; rows are vertically chained. |
| 7324–7348 | Begins tab-4 `UpdateSkillScreen`. After several shadowed/dead minifier locals, it snapshots skill points/data and counters, reverse-hides every type-6/7 row, clears `hasData`, collapses heights, and counts total achievement records. |
| 7349–7400 | Forward-scans unlocked entries (`skillPoints ≥ threshold`). Completed Fun/Achievement flags chain into the bright completed group; unlocked but incomplete rows chain into a dim/alternate-plate group. It reveals all populated row elements, anchors incomplete group below completed group or at top, formats title with unlocked/total/completed counts, and applies computed list height. |
| 7401–7411 | Repeats first-visible skill tutorial handling if `firstSkillShow` is still false, closes achievement refresh, replaces tab-4's earlier show script with it, and closes Feats factory `u`. |
| 7412 | Blank. |
| 7413–7446 | New `m` shadows captured `math.random` and begins About-screen construction. It creates a keyboard-enabled board-parented frame with bordered background; show pauses, shows three tabs, and hides game/summary/hint via alpha, while hide restores alpha. Installs `Bejeweled.aboutScreen`. |
| 7447–7495 | Creates three 133×34 About tabs with explicit `tabs` atlas coordinates and IDs 1–3. Tab 1 starts normal; tabs 2–3 start dimmed/one level lower. All use shared `U` mouse handler and are stored on About screen. |
| 7496–7500 | Creates/stores tab-1 About content frame, then begins a new child frame `n` at line 7500. About factory and child construction continue into batch 16. |

## Findings

- Feats of Skill construction is now complete. Tab 1 refreshes threshold-appropriate skill actions; tab 2 shows personal/fun statistics; tab 3 uses resolved leaderboard population; tab 4 groups unlocked achievements by completed state.
- Favorite-color tie handling is asymmetric: it recognizes only a tie between indices 1 and 2 when index 1 remains selected, so equal maxima involving other colors still display one color.
- Tab 4 temporarily receives a copied leaderboard `OnShow` that would call a missing method, but the factory overwrites it with achievement refresh at line 7410 before construction closes.
- Both skill and achievement `ScrollFrame` objects receive only `BackdropTemplate`, not the intended scroll-frame template, because of Lua `and` expression semantics.
- Achievement count caption arguments are unlocked count, total record count, then completed count. Display order is completed rows followed by unlocked/incomplete rows.
- About screen mirrors Feats screen pause/alpha behavior and begins a separate three-tab constructor that remains open.

## API and architecture mapping

- Legacy frame/scroll templates, generated checkbox font strings, texture atlas coordinates, `getglobal`, and keyboard propagation require authoritative Retail verification before implementation.
- `UI/Skills.lua`: completed Feats screen, stats, leaderboard, achievement plates, refresh/count/order behavior, and tutorial popup.
- `UI/About.lua`: open About factory `m`, screen visibility/pause behavior, and three-tab navigation shell.
- `Persistence/Stats.lua`: tab-2 mappings from profile/account stats to display, including favorite-color selection.

## Forward dependencies

Batch 16 must resume at line 7501 inside About tab-1 child construction, close factory `m` only when the source does, and continue sequentially through line 8000 without assuming later initialization order.
